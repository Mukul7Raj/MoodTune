# Production Requirements (Minimal - for Railway/Render deployment)
# Heavy ML packages (TensorFlow, OpenCV, FER) are excluded to avoid build failures
# The app will work without them - emotion detection will be limited but core features work

Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
python-dotenv==1.0.1
Werkzeug==2.3.7
gunicorn==21.2.0
requests==2.31.0
Pillow==10.0.0
numpy==1.24.3
typing-extensions>=4.8.0

# Optional ML packages (uncomment if needed - may cause build timeouts):
# opencv-python==4.8.1.78
# fer==22.5.1
# tensorflow==2.13.0
# opencv-contrib-python==4.8.1.78
# SpeechRecognition==3.10.0
# PyAudio==0.2.13
# moviepy==1.0.3
# mediadecoder==0.1.5
# mediapipe==0.10.9
